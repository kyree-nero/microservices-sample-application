spring:
  cloud:
    config:
      name: discovery
      uri: http://localhost:8081
      username: configUser
      password: configPassword
      fail-fast: true
logging:
  level:
    org:
      springframework:
        security: trace
        web: trace